// @artificer-forge/engine — root export: the composition component that wires the
// /core, /runtime and /ui layers into a ready-to-use game host. Import the layers
// directly (@artificer-forge/engine/{core,runtime,ui}) when you need their pieces.

export { default as Game } from './Game.vue'
