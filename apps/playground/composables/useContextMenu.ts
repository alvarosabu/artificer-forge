// Re-export shim: context-menu provide/inject now lives in @artificer-forge/engine/runtime.
export {
  type ContextMenuApi,
  ContextMenuKey,
  type ContextMenuState,
  useContextMenu,
  useContextMenuProvider,
} from '@artificer-forge/engine/runtime'
