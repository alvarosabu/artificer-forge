// Re-export shim: the portrait generator now lives in @artificer-forge/engine/runtime.
// Kept here so Nuxt auto-import keeps resolving usePortraitRenderer for its consumers.
export { usePortraitRenderer } from '@artificer-forge/engine/runtime'
