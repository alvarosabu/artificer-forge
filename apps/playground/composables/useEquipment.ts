// Re-export shim: useEquipment now lives in @artificer-forge/engine/runtime.
// Kept here so Nuxt auto-import keeps resolving it for existing consumers.
export { useEquipment } from '@artificer-forge/engine/runtime'
